# Synera 自走棋 PA 项目历程

本文档用于总结 Synera 自走棋项目从“阅读 PA 要求并从 0 搭建”到“形成较完整课程展示作品”的全过程。它不是简单的功能列表，而是按每一轮迭代说明：当时的目标是什么、为什么要这样改、具体改了什么、初学者可以从这一轮学到什么。

项目最终采用 Qt Widgets + C++17 + CMake 实现。核心逻辑集中在 `Game` 模型层，界面表现集中在 `GameWindow` 和 `ArenaWidget`，资源通过 `resources.qrc` 打包，基础测试通过 `--smoke-test` 和 `--ui-smoke-test` 验证。

## 一、整体开发思路

最开始的目标只是完成 PA 验收：有一个可运行的 Qt 图形界面，有棋盘，有单位，有基本交互，有战斗过程。但随着功能逐轮增加，项目逐渐形成了更像主流自走棋的结构：

- 模型层负责规则：棋子、棋盘、商店、金币、经验、羁绊、装备、强化、敌人、战斗结算。
- 表现层负责界面：棋盘绘制、拖拽、按钮、浮层、动画、音效、战报。
- 资源层负责素材：人物 sprite、音效、Qt 资源文件。
- 测试层负责稳定：命令行 smoke test 覆盖核心流程，UI smoke test 覆盖窗口创建和绘制路径。

这个划分很重要。自走棋看起来是一个“游戏界面项目”，但真正容易出问题的是规则状态：什么时候能买，什么时候能卖，单位在哪里，装备是否合成，战斗是否结束，保存读取后状态是否还一致。因此从一开始就需要把“游戏数据”和“界面显示”分开。

## 二、第 1 轮：阅读 PA 文档并规划从 0 开始实现

### 目标

阅读文件夹中的 PA 文档，理解课程要求，然后计划如何用 Qt 完成一个可以编译运行、可以验收展示的 GUI 项目。

### 修改与整合

这一轮主要是设计，不是立刻堆功能。规划时确定了几个基础方向：

- 使用 Qt Widgets，而不是额外引入游戏引擎。
- 使用 CMake 管理构建，方便 Qt Creator 识别项目。
- 使用 C++17 写模型层和界面层。
- 让 Qt Creator 使用已经安装在 C 盘默认路径下的 Qt 编译器和工具链。
- 项目结构分为 `src/`、`assets/`、`resources.qrc`、`CMakeLists.txt`、`README.md`。

### 原因

课程 PA 项目最重要的是稳定和可解释。如果一开始选择 Unity、Unreal 或复杂外部库，虽然视觉可能更强，但构建、提交和解释成本会大幅提高。Qt Widgets 的好处是：

- 窗口、按钮、绘图、事件处理都由 Qt 提供。
- `QPainter` 可以直接绘制棋盘和单位。
- `QTimer` 可以驱动战斗循环。
- Qt Creator 对 CMake 项目支持很好。

### 初学者可以学到什么

一个项目不要一开始就追求“最好看”，先确定技术边界。课程项目更适合先做稳定核心，再逐步美化。架构上要先想清楚：哪些是规则，哪些是显示，哪些是资源，哪些是测试。

## 三、第 2 轮：实现基础 Qt 自走棋项目

### 目标

执行初始计划，写出可以编译运行的基础项目。要求不是只有空窗口，而是有基本游戏功能。

### 修改与整合

这一轮建立了项目主体：

- 新建 `CMakeLists.txt`，配置 C++17、Qt Widgets、自动 MOC/RCC/UIC。
- 新建 `src/main.cpp`，作为程序入口。
- 新建 `src/game_model.h` 和 `src/game_model.cpp`，实现游戏数据和规则。
- 新建 `src/game_window.h` 和 `src/game_window.cpp`，实现 Qt 主窗口和自定义绘图控件。
- 建立基础棋盘、备战区、商店、单位、回合、战斗循环。

基础模型中逐渐形成了这些概念：

- `Owner`：区分我方和敌方。
- `Phase`：准备、战斗、结算、游戏结束。
- `UnitKind`：单位类型。
- `Trait`：羁绊类型。
- `EquipmentKind`：装备类型。
- `Unit`：棋子实体，包括血量、攻击、位置、装备、状态。
- `Game`：完整局内状态，包括棋盘、板凳、商店、金币、回合、战斗。

### 原因

自走棋不是简单拖拽游戏，它有状态机。准备阶段能买、卖、拖动；战斗阶段不能改变阵容；结算阶段要发奖励并进入下一回合。用 `Phase` 明确区分阶段，可以避免很多 bug。

### 初学者可以学到什么

写游戏项目时，先建立“状态”和“规则”，再考虑漂亮界面。比如拖拽棋子之前，先要知道棋子是否在棋盘上、是否属于玩家、当前是否允许移动。界面只是调用模型层 API，不应该自己偷偷改数据。

## 四、第 3 轮：解决 exe 双击缺少 DLL

### 目标

用户直接点击 exe 时提示缺少 DLL，需要补齐 Qt 运行依赖。

### 修改与整合

这一轮使用 Qt 自带的 `windeployqt` 工具，把运行需要的 DLL 和插件复制到 `build` 目录中，例如：

- Qt6Core.dll
- Qt6Gui.dll
- Qt6Widgets.dll
- platforms/qwindows.dll
- imageformats 插件
- styles 插件

后续加入音效后，又补充了 Qt Multimedia 相关 DLL 和插件。

### 原因

Qt 程序在 Qt Creator 中能运行，不代表双击 exe 也能运行。Qt Creator 会帮开发环境找到 DLL；但直接运行 exe 时，系统只会在 exe 旁边和 PATH 中找依赖。`windeployqt` 的作用就是把这些依赖部署到 exe 旁边。

### 初学者可以学到什么

“编译成功”和“发布运行成功”是两回事。Qt 项目交给别人或直接双击时，一般都要运行：

```powershell
windeployqt .\build\SyneraAutoArena.exe
```

这一步是 Windows Qt 项目非常常见的部署环节。

## 五、第 4 轮：规划进阶自走棋规则

### 目标

基础验收已经能完成，但项目仍像课程 Demo。用户希望它更接近网络上常见自走棋，因此先规划进阶规则。

### 计划内容

规划包括：

- 商店概率和共享英雄池。
- 金币经济、利息、连胜连败收益。
- 购买经验和人口上限。
- 1 星合 2 星、2 星合 3 星。
- 更多英雄、更多羁绊。
- 装备基础件和高级装备合成。
- PvE 野怪和 Boss 回合。
- 强化符文系统。
- 战斗统计和结算面板。

### 原因

主流自走棋的核心乐趣不只是“棋子互打”，而是玩家在每回合做选择：

- 花钱刷新还是存钱吃利息？
- 买经验上人口还是追三星？
- 当前阵容缺哪个羁绊？
- 装备给前排还是输出？
- 面对下一波敌人是否要换站位？

这些规则决定了游戏是否有策略感。

### 初学者可以学到什么

功能扩展要分优先级。先补核心规则，再补表现。对于自走棋，优先级一般是经济、商店、人口、升星、羁绊、装备，然后才是动画和音效。

## 六、第 5 轮：实现进阶规则

### 目标

把上一轮规划中的主流自走棋规则落到代码里。

### 修改与整合

模型层扩展了大量规则：

- `RuleConfig` 集中管理刷新费用、经验费用、利息上限等规则参数。
- 商店根据玩家等级产生不同费用段的英雄。
- 英雄池使用共享数量池，购买减少，出售返还。
- 商店支持刷新和锁定。
- 金币系统支持基础收入、利息、连胜/连败收益。
- 人口从“直接升级”改为“购买经验，经验满后升级”。
- 英雄从 5 个扩展到 15 个。
- 羁绊扩展到 8 种。
- 装备扩展为 8 个基础件和 8 个高级装备。
- 增加强化符文，例如经济类、战斗类、装备类。
- 战斗中加入护甲、魔抗、暴击、闪避、吸血、护盾、控制等属性。
- 保存读取加入更多字段，保证新增状态可保存。

### 原因

这些功能让项目从“能演示”变成“能玩几回合”。例如：

- 共享英雄池让买卖有意义。
- 经验系统让玩家在升级和刷新之间选择。
- 羁绊让阵容搭配有目标。
- 装备合成让 PvE 掉落更有价值。
- 强化符文让每局有不同方向。

### 初学者可以学到什么

新增规则时，不要把规则散落在界面按钮里。正确做法是让 `Game` 提供公开方法，例如 `buyFromShop`、`refreshShop`、`buyXp`、`sellUnit`、`equipUnit`。界面只负责调用这些方法并刷新显示。

## 七、第 6 轮：将 UI 文本统一为中文

### 目标

用户希望界面展示都改成中文，如果不会造成负面影响就直接修改。

### 修改与整合

这一轮把界面文案和游戏状态文案尽量改为中文，例如：

- 阶段显示：准备、战斗、结算。
- 商店、羁绊、装备、日志、设置。
- 状态提示、购买提示、金币不足提示。
- 单位名称、装备名称、羁绊名称、技能名称。

同时注意中文字符串在 Windows/MinGW 下可能乱码，因此后续逐步采用 `QStringLiteral` 或保证源码 UTF-8。

### 原因

这是课程展示项目，中文 UI 更容易让老师和同学理解功能。只要代码标识符仍保持英文，中文 UI 不会影响项目结构。

### 初学者可以学到什么

代码里的类名和函数名建议用英文，界面给用户看的文字可以本地化。这样既方便开发，也方便展示。

## 八、第 7 轮：规划 GUI 与战斗表现美化

### 目标

当前玩法有了，但战斗反馈不够清楚。用户希望继续美化 GUI 和战斗过程。

### 计划内容

规划包括：

- 统一暗色策略界面。
- 顶部状态栏突出生命、金币、利息、等级、经验、人口、回合。
- 棋盘增加悬停高亮、可放置提示、半场区分。
- 商店卡片增加费用颜色、羁绊标签、售罄灰化。
- 新增 `VisualEvent` 表现事件。
- 浮动伤害数字、治疗数字、暴击、闪避、复活提示。
- 单位移动改为插值动画。
- 普攻弹道和技能范围特效。
- 结算面板显示本轮表现。
- 设置面板加入动画速度、坐标显示、自动下一回合等选项。

### 原因

自走棋战斗是自动进行的，如果玩家看不清谁在打谁、谁放了技能、为什么赢或输，策略感会下降。视觉反馈不是装饰，而是帮助玩家理解规则。

### 初学者可以学到什么

动画不应该改变规则。正确做法是模型层产生“发生了什么”的事件，表现层根据事件画动画。比如伤害已经结算完成，UI 再画浮字；不要让浮字动画决定伤害。

## 九、第 8 轮：实现 GUI 与战斗表现美化

### 目标

把视觉事件、战斗反馈和界面层次真正实现出来。

### 修改与整合

核心改动包括：

- 增加 `VisualEventType` 和 `VisualEvent`。
- 战斗伤害、治疗、技能、复活、装备合成都可以产生视觉事件。
- `ArenaWidget` 绘制浮字、弹道、技能圈、单位血条和状态。
- 单位绘制中加入星级徽章、装备点、控制状态标记。
- 结算时显示更完整的战斗结果。
- 设置中提供动画速度选项。

### 原因

这一轮让战斗过程从“数字在后台变化”变成“玩家能看见发生了什么”。比如一个技能造成伤害时，模型层调用 `damage`，同时添加视觉事件；UI 在下一帧读取事件并绘制。

### 初学者可以学到什么

`QTimer` + `paintEvent` 是 Qt 动画的基础模式：

- `QTimer` 定期更新状态。
- `update()` 请求重绘。
- `paintEvent` 用 `QPainter` 绘制当前帧。

不要在 `paintEvent` 里直接修改核心游戏规则，否则会让逻辑变得混乱。

## 十、第 9 轮：规划降低战斗速度并加入人物资源

### 目标

用户觉得战斗过程还可以更慢、更清楚，并希望联网搜索免费资源加入人物形象。

### 计划内容

规划包括：

- 默认动画速度改为 0.5x。
- 战斗视觉事件持续更久。
- 引入免费或 CC0 的人物 sprite。
- 新增 `assets/units/`、`assets/enemies/`、`assets/ui/`、`assets/effects/`。
- 新增 `resources.qrc`，把 PNG 打包进 exe。
- 棋盘单位、商店卡、单位详情都显示角色图。
- 找不到素材时回退到 QPainter 绘制，不崩溃。
- README 记录素材来源和许可证。

### 原因

课程展示时，老师不一定能快速理解高速战斗。降低默认速度可以让演示更清楚。人物资源能让界面从“抽象色块”变成“游戏棋子”，但资源必须有许可说明，避免提交时解释不清。

### 初学者可以学到什么

使用外部资源时要考虑两件事：

- 技术上如何加载：Qt 可以用 `resources.qrc` 把资源编进程序。
- 法律上是否可用：README 要写清来源和 license。

## 十一、第 10 轮：实现素材层和慢速战斗

### 目标

接入人物素材，降低战斗节奏，让棋盘、商店和详情面板显示角色形象。

### 修改与整合

实际实现中加入了：

- `assets/units/RPGCharacterSprites32x32.png`。
- `assets/units/iso/` 下的等距角色备用资源。
- `resources.qrc` 打包素材。
- `ArenaWidget` 中加入图片缓存，避免每帧重复加载。
- 单位绘制优先使用 sprite，缺失时 fallback 到程序绘制。
- 商店卡片和单位详情显示头像。
- 默认动画速度调慢。
- README 增加素材来源和许可说明。

### 原因

资源接入不能让程序变脆弱。如果某张图缺失，程序仍应显示备用 token，而不是白屏或崩溃。因此 UI 层使用“先尝试加载图片，失败就程序绘制”的策略。

### 初学者可以学到什么

资源加载应当有 fallback。尤其是课程项目，演示时最怕“因为一张图片路径错了导致程序崩溃”。好的做法是把素材当作表现增强，而不是核心依赖。

## 十二、第 11 轮：规划 2.5D、攻击动画与音效

### 目标

用户希望界面变成 2.5D，人物尽量立体，有攻击动作，最好有声音。

### 计划内容

规划时判断：真 3D 骨骼模型在 Qt Widgets 项目中成本过高，因此采用“伪 2.5D 等距/正面绘制 + sprite + QPainter 特效”。

计划包括：

- 棋盘从平面改为 2.5D。
- 单位增加阴影、描边、脚底锚点。
- 普攻时单位轻微前倾或挥动。
- 远程单位增加弹道。
- 技能增加圆环、光柱、冲击波。
- CMake 加入 Qt Multimedia。
- 使用 `QSoundEffect` 播放攻击、技能、治疗、金币、胜利、失败音效。
- 设置面板加入音量和静音。

### 原因

真正 3D 需要模型加载、骨骼动画、相机、灯光、深度排序、材质和动画混合，不适合当前 PA 项目。伪 2.5D 能用较低成本提升观感，同时保持稳定。

### 初学者可以学到什么

技术选择要服务目标。用户说“想要 3D”，不一定必须马上引入 3D 引擎。可以先分析成本，再选择能达到展示目标的方案。

## 十三、第 12 轮：实现 2.5D、职业特效和音效

### 目标

让战斗看起来更像游戏：棋盘有空间感，单位有攻击反馈，操作有声音。

### 修改与整合

主要改动包括：

- `CMakeLists.txt` 增加 `find_package(Qt6 REQUIRED COMPONENTS Widgets Multimedia)`。
- 链接 `Qt6::Multimedia`。
- `assets/sfx/` 增加短 WAV 占位音效。
- `resources.qrc` 打包音效。
- `ArenaWidget` 中加入音效管理，支持静音和音量。
- 增加职业攻击风格：
  - 战士：刀光。
  - 法师：法阵和魔法弹。
  - 游侠：箭矢。
  - 刺客：短残影。
  - 守卫：护盾波纹。
  - 工匠：机械脉冲。
  - 辅助：治疗环。
- 单位绘制加入阴影、血条、蓝条、星级、装备点。

### 原因

这一轮的重点不是改变战斗数值，而是让同样的规则更容易被观察。比如战士和法师造成伤害的模型逻辑可以类似，但表现上应让玩家一眼看出职业差异。

### 初学者可以学到什么

表现层可以有“职业风格”，但不要让表现层决定伤害。攻击动画、刀光、音效都只是反馈；真正的伤害仍由 `Game::damage` 和战斗循环计算。

## 十四、第 13 轮：规划主界面重排与正面战场

### 目标

用户觉得界面太小，而且斜着的棋盘观感较差。希望战场放到中心，我方半场正对屏幕，其他信息像 MOBA 商店一样通过按钮打开。

### 计划内容

规划包括：

- 默认窗口扩大到 1440x900，最小尺寸提高到 1200x760。
- 棋盘居中并按可用空间动态放大。
- 从斜向等距棋盘改为正面 2.5D 棋盘。
- 主屏只保留核心 HUD：生命、金币、经验、人口、回合、阶段。
- 商店、羁绊、单位、装备、日志、设置改为右侧按钮。
- 点击按钮打开浮层，再点关闭。
- 浮层不改变棋盘尺寸。
- 准备阶段允许操作，战斗阶段禁止拖动但允许查看信息。

### 原因

之前为了展示很多功能，界面上常驻内容太多，导致主战场变小。自走棋的主视觉应该是棋盘，商店和信息面板应当按需打开。

### 初学者可以学到什么

UI 设计不是把所有功能都摆出来。信息越多，越需要分层：

- 高频信息常驻。
- 中频操作用按钮打开。
- 低频设置放在设置面板。

## 十五、第 14 轮：实现主界面重排与浮层系统

### 目标

把主界面改成“战场居中 + 右侧按钮 + 浮层面板”的结构。

### 修改与整合

主要实现包括：

- `ArenaWidget` 增加 `PanelMode`。
- 右侧按钮包括商店、羁绊、单位、装备、日志、设置、强化。
- 点击按钮切换浮层。
- 商店卡片移动到商店浮层。
- 羁绊信息移动到羁绊浮层。
- 装备池移动到装备浮层。
- 战斗日志移动到日志浮层。
- 设置项移动到设置浮层。
- 棋盘采用正面 2.5D 投影。
- 拖拽命中仍转换回原来的 8x8 逻辑格。

### 原因

这轮没有改变游戏规则，而是重组信息架构。核心目标是让主屏看起来像真正游戏界面，而不是开发调试面板。

### 初学者可以学到什么

UI 重构时要保护模型层。棋盘显示方式改了，但 `Game` 中的 `board[row][col]` 仍然是 8x8。这样 AI、BFS、保存读取、战斗逻辑都不需要重写。

## 十六、第 15 轮：讨论真 3D 人物建模的困难

### 目标

用户提出网络上常见自走棋人物是 3D 模型，并希望不同职业有不同攻击动作，例如战士挥砍、法师举杖。

### 反馈与计划

这一轮明确说明：真正 3D 骨骼模型不好做，主要困难包括：

- Qt Widgets + QPainter 不能直接加载和播放 3D 骨骼动画。
- 需要 OpenGL、Qt Quick 3D 或额外模型引擎。
- 需要处理模型格式，如 FBX、GLB。
- 不同模型的骨骼、材质、比例、动作很难统一。
- 构建和部署复杂度会大幅提高。

因此建议采用“看起来像 3D 的 2.5D sprite 和职业动作特效”。

### 原因

课程项目的第一目标是稳定交付。真 3D 会带来过多不可控因素，而伪 3D 可以满足展示中的大部分观感需求。

### 初学者可以学到什么

需求分析中要敢于说明困难。不是所有“想要的效果”都适合当前技术栈。合理方案通常是在效果、成本、稳定性之间折中。

## 十七、第 16 轮：规划精细准 3D 人物与职业动作资源

### 目标

用户希望在不做真 3D 的基础上，让人物模型更精细，看起来更像 3D，并自行搜索资源。

### 计划内容

规划包括：

- 使用 CC0 的等距/低多边形风格角色资源。
- 建立 `UnitVisualDef`，管理单位视觉定义。
- 建立 `AttackStyle`，管理职业攻击风格。
- 棋盘单位显示动画帧。
- 不同职业使用不同攻击表现：
  - 战士挥砍。
  - 法师施法。
  - 游侠射箭。
  - 刺客突进。
  - 守卫盾击。
  - 工匠脉冲。
  - 辅助治疗。
- 商店和详情仍使用头像式展示。

### 原因

预渲染 sprite 是很多 2D/2.5D 游戏的常见做法。它不需要实时 3D 引擎，但可以呈现较立体的角色。

### 初学者可以学到什么

“数据表驱动表现”很重要。不要在绘制函数里到处写 `if 英雄 A 就画红色，英雄 B 就画蓝色`。更好的做法是用视觉定义表集中管理路径、颜色、帧、缩放、职业风格。

## 十八、第 17 轮：实现准 3D 人物后发现观感问题

### 目标

尝试把人物画得更立体，加入更复杂的伪 3D 身体和职业动作。

### 修改与整合

这一轮曾尝试：

- 更复杂的人物身体绘制。
- 伪 3D 的头、身体、四肢、武器。
- 职业动作带动身体变化。
- 更夸张的攻击表现。

### 出现的问题

用户反馈人物“太丑”“看起来很怪”“没有正常自走棋那种感觉”。

问题原因是：用 `QPainter` 拼接假 3D 人体很容易产生不协调感。自走棋棋子通常不是完整写实模型，而是统一风格的棋子、底座、头像、特效组合。强行画假 3D 人体反而破坏了整体风格。

### 初学者可以学到什么

不是功能越复杂越好。视觉设计中，“统一、清楚、稳定”比“强行高级”更重要。用户反馈要及时吸收，不能为了坚持技术方案而牺牲观感。

## 十九、第 18 轮：修正人物观感，改为精致自走棋棋子

### 目标

撤掉怪异伪 3D 人体，改成正常自走棋棋子风格。

### 修改与整合

本轮改动包括：

- 停用 `drawProfessionalBody` 的调用。
- 棋盘单位改为“角色 sprite + 精致底座 + 阴影 + 血蓝条”。
- 加入圆形/金属感底座。
- 加入阵营描边。
- 星级、装备、控制状态放在棋子外缘。
- 敌方单位用红色描边和暗色叠层，不再换奇怪模型。
- 保留职业攻击特效，但单位本体只做轻微位移，不再变形。

### 原因

主流自走棋的棋子需要统一可读。玩家最先要看清：

- 哪个是我方，哪个是敌方。
- 单位血量和蓝量。
- 单位星级。
- 是否装备了物品。
- 是否处于控制状态。

因此棋子设计应服务识别，而不是强行表现复杂人体。

### 初学者可以学到什么

视觉迭代中要能回退。发现方案不好看时，保留有用部分，例如职业特效和 sprite 资源，删除或停用不合适部分，例如伪 3D 身体。

## 二十、第 19 轮：规划从“好看可玩”升级为“策略完整”

### 目标

用户询问继续完善自走棋可以从哪些方面做。此时视觉已经比较完整，下一步重点转向策略反馈和局内目标感。

### 计划内容

规划包括：

- 战斗结算升级为正式战报。
- 日志浮层增加战斗、经济、装备、系统信息。
- 单位详情显示本轮表现和推荐定位。
- 增加阵容建议：最强羁绊、下一档差几个英雄、人口是否溢出。
- 装备浮层增加合成树。
- 敌人从固定波次升级为阵容模板库。
- 战斗前显示敌人预告。
- 首次购买、首次上阵、首次装备、首次合成、首次强化显示短提示。
- 扩展 smoke test。

### 原因

视觉继续堆下去收益变低。真正让自走棋“像完整游戏”的，是玩家能理解自己的局势：

- 为什么赢？
- 为什么输？
- 哪个单位贡献最大？
- 当前阵容差什么？
- 下一波敌人是什么？
- 装备能合成什么？

这些反馈能帮助玩家做策略决策。

### 初学者可以学到什么

项目后期不要只追求新功能数量。要思考玩家是否理解已有功能。好的结算面板和阵容建议，往往比多加几个英雄更能提升完成度。

## 二十一、第 20 轮：实现策略完整度升级

### 目标

实现战报、阵容分析、装备合成树、敌人模板预告、新手提示和更完整测试。

### 修改与整合

模型层增加：

- `enemyPreviewText()`：返回下一波敌人预告。
- `strategyAdviceText()`：返回阵容建议。
- `lastDropText()`：返回上一轮掉落信息。
- `enemyPreview` 字段：保存敌人预告。
- `lastDrop` 字段：保存掉落记录。

`generateEnemies()` 从固定敌人改为主题模板：

- 战士前排。
- 游侠后排。
- 法师爆发。
- 刺客切后。
- 工匠守卫。
- Boss 试炼。

战斗结算中增加：

- 胜负结果。
- 金币来源。
- 掉落装备。
- MVP。
- 输出、治疗、承伤排行。

UI 层增加：

- `drawLogPanel` 显示正式战报。
- `drawTraitsPanel` 显示阵容建议。
- `drawUnitPanel` 显示定位推荐和装备提示。
- `drawEquipment` 显示装备池和合成树。
- `drawCoachTip` 显示角落新手提示。

测试层增加：

- `--smoke-test` 覆盖买卖、锁店、刷新、经验、保存读取、装备合成、强化选择、敌人生成、战斗和阵容建议。
- `--ui-smoke-test` 创建 Qt 窗口并短暂处理事件，验证关键绘制路径不崩溃。

README 增加：

- 新增验证命令。
- 新增策略反馈说明。
- 新增敌人模板、装备 UX、教练提示说明。

### 原因

这一轮重点是“解释性”。PA 展示时，老师不仅要看到游戏能跑，还要能理解项目有规则、有策略、有测试、有扩展能力。

### 初学者可以学到什么

测试不仅是为了找 bug，也是为了证明项目稳定。一个好的 smoke test 不一定覆盖所有细节，但要覆盖主要流程：买、卖、装备、强化、保存、读取、开战、结算。

## 二十二、最终项目结构总结

### `CMakeLists.txt`

负责配置项目构建：

- C++17。
- Qt Widgets。
- Qt Multimedia。
- 自动处理 MOC、RCC、UIC。
- 把 `resources.qrc` 编进 exe。

初学者要理解：CMake 是项目的“构建说明书”。Qt Creator 能不能识别项目，编译器能不能找到 Qt，都和它有关。

### `src/main.cpp`

负责程序入口和测试入口：

- 普通运行时创建 `QApplication` 和 `GameWindow`。
- `--smoke-test` 运行模型层测试。
- `--ui-smoke-test` 运行 UI 创建测试。

初学者要理解：同一个 exe 可以根据命令行参数进入不同模式。这让测试更方便。

### `src/game_model.h` 与 `src/game_model.cpp`

负责游戏核心规则：

- 棋盘、板凳、商店。
- 单位、装备、羁绊、强化。
- 金币、经验、人口。
- 战斗循环、伤害、治疗、技能。
- 敌人生成、结算、掉落。
- 保存读取。

初学者要理解：模型层不应该依赖按钮位置、颜色、鼠标坐标。它只关心游戏规则。

### `src/game_window.h` 与 `src/game_window.cpp`

负责 Qt 界面：

- 主窗口。
- 棋盘绘制。
- 拖拽交互。
- 商店、羁绊、单位、装备、日志、设置浮层。
- 战斗动画、视觉事件、音效。
- 新手提示。

初学者要理解：界面层可以很复杂，但它的职责是“显示和交互”，不要把核心规则写死在绘制函数里。

### `resources.qrc`

负责资源打包：

- 人物图片。
- 等距备用图片。
- 音效。

初学者要理解：Qt 资源系统可以让图片和音频跟随 exe，不需要运行时到处找文件。

### `README.md`

负责项目说明：

- 如何构建。
- 如何运行。
- 如何测试。
- 项目功能。
- 素材来源和许可证。
- AI 使用说明。

初学者要理解：README 是交付的一部分。老师或别人拿到项目后，首先看的通常就是 README。

## 二十三、关键技术学习路线

### 1. 先学 Qt 程序基本结构

先理解：

- `QApplication`。
- `QMainWindow`。
- `QWidget`。
- `paintEvent`。
- `mousePressEvent`、`mouseMoveEvent`、`mouseReleaseEvent`。
- `QTimer`。

本项目中，`GameWindow` 是主窗口，`ArenaWidget` 是主要绘制区域。

### 2. 再学模型层状态设计

重点理解：

- 为什么需要 `Phase`。
- 为什么棋盘用二维数组。
- 为什么板凳用数组。
- 为什么单位用 ID 查找。
- 为什么公开 API 比直接改字段更安全。

例如拖动单位时，界面不应该直接写 `board[row][col] = id`，而应该调用 `moveBoardToBoard` 或 `moveFromBenchToBoard`。

### 3. 学会把规则做成函数

例如：

- `buyFromShop` 负责购买。
- `sellUnit` 负责出售。
- `buyXp` 负责经验。
- `equipUnit` 负责装备。
- `tryCombineEquipment` 负责合成。
- `applySynergies` 负责羁绊。
- `startCombat` 负责进入战斗。

每个函数只做自己的事情，项目就更容易维护。

### 4. 学会把表现和规则分开

战斗中，伤害由模型层计算；浮字、弹道、音效由表现层显示。这个分离非常关键。

如果表现层直接决定伤害，会导致：

- 动画速度影响战斗结果。
- 暂停或掉帧改变数值。
- 测试很难写。

本项目中，动画速度只影响观看体验，不改变核心结算。

### 5. 学会保存读取

保存读取要包含：

- 玩家状态。
- 单位列表。
- 棋盘和板凳。
- 商店。
- 装备。
- 强化。
- 敌人预告和掉落记录。

读取旧存档时，如果字段不存在，应使用默认值。这就是兼容性。

### 6. 学会做 smoke test

本项目的 `--smoke-test` 不打开 GUI，而是直接运行模型层流程：

- 买单位。
- 卖单位。
- 锁店。
- 刷新。
- 买经验。
- 存档。
- 读档。
- 装备合成。
- 选择强化。
- 开始战斗。
- 更新若干帧。
- 检查阵容建议。

这种测试可以快速发现规则层是否被改坏。

### 7. 学会部署 Qt 程序

在 Windows 上，构建后要运行：

```powershell
windeployqt .\build\SyneraAutoArena.exe
```

如果加入 Qt Multimedia，也要确认 Multimedia 插件被复制。否则音效可能不能工作，或者 exe 双击缺少 DLL。

## 二十四、重要经验总结

### 经验 1：先完成可运行，再追求好看

如果一开始就做 2.5D、音效、复杂 UI，很容易没有稳定核心。正确顺序是：

1. 基础棋盘和单位。
2. 基础战斗。
3. 商店、经济、人口。
4. 羁绊、装备、强化。
5. UI 和动画。
6. 战报、建议、教程。

### 经验 2：每次扩展都要问“是否破坏已有功能”

例如加入浮层 UI 时，不能破坏拖拽；加入 2.5D 棋盘时，不能破坏 8x8 逻辑格；加入音效时，不能影响战斗结算。

### 经验 3：视觉方案不好看时要敢于调整

伪 3D 人体方案被反馈怪异后，项目没有继续硬做，而是改为更统一的自走棋 token。这个决定让界面更干净，也更适合课程展示。

### 经验 4：文档和测试也是项目的一部分

README、素材许可、PA 历程、smoke test 都不是附属品。它们能证明项目是认真完成的，也能帮助答辩时解释实现过程。

### 经验 5：不要把功能写死

项目多次强调后续可能加入声音、装备合成树、2.5D、更多英雄、更多敌人。为了支持扩展，项目使用了：

- 枚举和定义表。
- `RuleConfig`。
- `VisualEvent`。
- `PanelMode`。
- `AttackStyle`。
- 资源 qrc。
- 模型层公开 API。

这些设计让后续扩展更容易。

## 二十五、最终验收建议

展示时可以按下面顺序操作：

1. 打开程序，说明这是 Qt Widgets + C++17 实现。
2. 展示主界面：中间战场，右侧浮层按钮，底部操作。
3. 打开商店，购买单位。
4. 拖动单位上阵，说明准备阶段和战斗阶段不同。
5. 打开羁绊面板，说明阵容建议和羁绊档位。
6. 开始战斗，展示浮字、弹道、技能、音效。
7. 打开日志面板，展示战报、经济拆分、MVP 和排行。
8. 展示装备面板，说明基础装备和合成树。
9. 展示强化符文。
10. 保存读取或运行 smoke test，说明项目稳定性。

命令行验证：

```powershell
cmake --build build
.\build\SyneraAutoArena.exe --smoke-test
.\build\SyneraAutoArena.exe --ui-smoke-test
```

部署验证：

```powershell
windeployqt .\build\SyneraAutoArena.exe
```

如果 `windeployqt` 提示 `dxcompiler.dll` 和 `dxil.dll` 缺失，一般不影响当前 Qt Widgets/QPainter 主流程。只要程序能打开，smoke test 通过，基本验收不受影响。

## 二十六、结语

Synera 的开发过程体现了一个课程 PA 项目常见的成长路线：先满足基础要求，再补规则深度，再补界面表现，最后补测试、文档和可解释性。最终项目不是简单堆功能，而是形成了比较清楚的结构：

- `Game` 管规则。
- `ArenaWidget` 管绘制和交互。
- `resources.qrc` 管资源。
- `main.cpp` 管入口和测试。
- `README.md` 和本文档管说明和历程。

对于初学者来说，最值得学习的不是某一个特效怎么画，而是整个实现顺序：先搭框架，再做模型，再接 UI，再加表现，最后用测试和文档收尾。这样的顺序能让项目在不断扩展时仍然保持稳定。
