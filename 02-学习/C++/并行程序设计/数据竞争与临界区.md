# 数据竞争与临界区
- **数据竞争:当多个进程或线程同时访问共享数据,至少有一个任务会修改数据的场景**
- **临界区:访问共享数据的代码片段**
## 1.数据竞争的例子
![[Pasted image 20260608144440.png]]
![[Pasted image 20260608144426.png]]
- 上面是单线程运行,结果没有什么问题,下面是多线程运行,结果出现问题
首先要明确一个问题: 当子线程创建的时候子线程就开始运行,然后`join()`的意思是主线程等待子线程完成,当多个子线程同时`join()`的时候,子线程并不会阻塞子线程,所以这个join循环只保证所有子线程运行完才会继续主线程,子线程之间依旧存在竞争
